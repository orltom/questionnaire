package: api

generate:
  std-http-server: true
  models: true

output-options:
  type-mapping:
    string:
      formats:
        uuid:
          type: uuid.UUID
          import: uuid

output: server.gen.go